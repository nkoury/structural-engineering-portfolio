# Expanded Background Site

This folder preserves the previous expanded version of the portfolio. It includes the Process page,
resume contact section, process asset gallery, and the fuller content editor.

The backup site shares the root `assets/` directory. Open `expanded-background-site/index.html` or
publish from the repository root and visit `/expanded-background-site/` to view this version.
