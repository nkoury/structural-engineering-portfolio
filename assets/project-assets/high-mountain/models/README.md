# High Mountain Models

Place public-safe web model files here.

Preferred:

- `.glb` for a stable single-file browser viewer
- `.gltf` plus `.bin` only when split files are needed

Keep confidential source models and original U3D files outside the public repository unless they are intentionally public.
